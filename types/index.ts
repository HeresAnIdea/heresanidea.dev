export interface Idea {
  id: number;
  title: string;
  description: string;
  author: string; // assuming this is a string, adjust as necessary
  // Add other fields as per your backend structure.
}
